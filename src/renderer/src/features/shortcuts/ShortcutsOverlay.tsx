import type { ReactElement } from 'react';
/** Owned by the Interactions track. Rendered when store.overlay === 'shortcuts'. */
export function ShortcutsOverlay(): ReactElement | null {
  return null;
}
