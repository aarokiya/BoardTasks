import type { ReactElement } from 'react';
/** Owned by the Interactions track. Root of the floating Quick Add window (boot.window === 'quickadd'). */
export function QuickAddApp(): ReactElement | null {
  return null;
}
