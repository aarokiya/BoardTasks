import type { ReactElement } from 'react';
/** Owned by the Interactions track. Rendered by AppShell when store.overlay === 'palette'. */
export function CommandPalette(): ReactElement | null {
  return null;
}
